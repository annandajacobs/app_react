import './Banner.css'

function Banner() {
    // JSX = como o react lê o código e transforma em document
    return (
        <header className="banner">
            <img src="/imagens/banner.png" alt="O banner principal da página do Organo." />
        </header>
        
    )
}

export default Banner;